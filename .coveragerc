[run]
branch = true
# The tool is an extensionless script imported by path, so filter by filename
# rather than module name.
include =
	*/git-third-party

[report]
show_missing = true
# The suite drives every branch in-process; regressions must not slip through.
fail_under = 100
